# den-ai-music-starter
Starter web app for AI music generation — backend + frontend, job queue, and provider adapter. Generated by Den assistant.

## Extra tool in this repo

In addition to the starter app, this repo includes `suno_prompt_builder.py` with 13 CLI modes:

1. `suno` → short Suno-friendly style prompt
2. `mastering` → SENTIVOX/V5 mastering report
3. `v6-suno` → extended V6 Suno analysis
4. `aurion` → AURION/HYPERSONA V∞ intent output
5. `sentivox` → V555 vocal prompt structure with fixed sections
6. `voicebrain` → persona design
7. `arcify` → emotional vocal arc
8. `lyricmatch` → lyric intent to vocal delivery
9. `abtest` → A/B comparison of vocal approaches
10. `producer` → V222 production direction output
11. `songwriter` → V222 songwriter reality-architect output
12. `omega` → OMEGA system (AURION + MIXBOARD + SENTIVOX)
13. `styleprompt` → direct Suno style prompt text (e.g. 1000 chars)

### V555 SENTIVOX example

```bash
python suno_prompt_builder.py sentivox \
  --style "dark electronic cinematic" \
  --emotion "haunted warmth" \
  --vocal-type "androgynous airy clean" \
  --language "en"
```

`sentivox` output always includes:
- `[PERSONA]` — with style context
- `[EMOTIONAL ARC]`
- `[VOCAL MAP]`
- `[STYLE CONTEXT]`
- `[ALTERNATIVES]`
- `[SELF-OPTIMIZATION]`


### OMEGA example

```bash
python suno_prompt_builder.py omega \
  --mood "neon rain city" \
  --emotion "melancholic hope" \
  --vocal "androgynous intimate whisper" \
  --style "glitchy synthwave cinematic" \
  --tempo "mid-slow pulse" \
  --image "time-fracture skyline in rain" \
  --export-goal "streaming + storytelling" \
  --language "en"
```

OMEGA output format:
- `[ AURION OUTPUT ]` — includes World, Emotion Curve, Symbolic Set, Vocal Soul, **Tempo**, Lyrics, Meta-Truth (localized to `--language`)
- `[ OMEGA MIXBOARD ]`
- `[ SENTIVOX MASTER ]`

### Running tests

```bash
pip install -r requirements-dev.txt
python -m pytest test_suno_prompt_builder.py -v
```
